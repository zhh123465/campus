# =============================================================================
# Testcontainers 配置示例
# =============================================================================
#
# 将本文件复制为 .testcontainers.properties（仓库根或 ~/.testcontainers.properties）
# 并根据你的 Docker 环境修改。
#
# 注意：.testcontainers.properties 已被 .gitignore 排除，不会被误提交。
#
# 更多配置项参考：
#   https://java.testcontainers.org/features/configuration/
# =============================================================================

# Docker daemon 连接方式（默认使用本地 socket）
# 如果你在 Windows/macOS 上使用远程 Linux VM 中的 Docker，取消注释并修改：
#docker.host=tcp://192.168.x.x:2375

# 如果 Docker daemon 使用 TLS：
#docker.host=tcp://192.168.x.x:2376
#docker.tls.verify=1
#docker.cert.path=/path/to/certs

# Ryuk（资源回收容器）配置
# 如果你的环境不支持 Ryuk（如某些 CI 环境），可以禁用：
#ryuk.disabled=true

# Testcontainers Cloud（如果使用 Testcontainers Cloud 服务）：
#tc.cloud.token=your-token-here
